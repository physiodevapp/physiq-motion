name: Deploy to PhysiQ hub

on:
  push:
    branches: [main]

concurrency:
  group: physiq-hub-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout physiq-motion
        uses: actions/checkout@v4

      - name: Checkout physiq hub
        uses: actions/checkout@v4
        with:
          repository: physiodevapp/physiq
          token: ${{ secrets.PHYSIQ_DEPLOY_TOKEN }}
          path: physiq-hub

      - name: Copy files to hub
        run: |
          rm -rf physiq-hub/motion
          mkdir -p physiq-hub/motion/lib physiq-hub/motion/icons
          cp index.html app.js sw.js style.css manifest.json favicon.svg .nojekyll physiq-hub/motion/
          cp icons/icon-192.png icons/icon-512.png physiq-hub/motion/icons/
          cp lib/session.js physiq-hub/motion/lib/
          cp lib/regions.js physiq-hub/motion/lib/

      - name: Commit and push
        working-directory: physiq-hub
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A motion/
          git diff --cached --quiet || git commit -m "chore: update motion from physiq-motion@${{ github.sha }}"
          for i in 1 2 4 8; do
            git push && break
            echo "Push rejected, retrying in ${i}s..."
            sleep $i
            git pull --rebase origin main
          done
